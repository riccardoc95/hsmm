% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main_fit.R
\name{fit_hsmm}
\alias{fit_hsmm}
\title{Fit a Hidden Semi-Markov Model (HSMM)}
\usage{
fit_hsmm(
  data,
  n_states,
  covariates_omega = NULL,
  covariates_q = NULL,
  semi = TRUE,
  max_dwell = NULL,
  model_type = "torus",
  max_iter = 100,
  tol = 1e-05,
  verbose = TRUE,
  init = NULL,
  seed = NULL
)
}
\arguments{
\item{data}{Matrix or data frame containing the observed sequence. Rows
correspond to time points and columns to observed variables.}

\item{n_states}{Number of latent states of the model.}

\item{covariates_omega}{Optional covariates for the emission model
\code{omega}. Must have the same number of rows as \code{data}.}

\item{covariates_q}{Optional covariates for the dwell-time model \code{q}.}

\item{semi}{Logical. If \code{TRUE} (default) the fitted model is an HSMM;
if \code{FALSE} it reduces to a standard HMM.}

\item{max_dwell}{Maximum dwell time (in number of observations) allowed for
each state in the semi-Markov formulation. If \code{NULL}, a default value
of 1 is used.}

\item{model_type}{Name of the model specification to use for the emission
component (for example \code{"torus"}, \code{"gaussian"}, etc.).}

\item{max_iter}{Maximum number of EM iterations.}

\item{tol}{Convergence tolerance for the EM algorithm.}

\item{verbose}{Logical value indicating whether to print progress messages
during training.}

\item{init}{Optional list with initial parameter values. If \code{NULL},
starting values are determined automatically.}

\item{seed}{Numeric value used to set the random-number generator seed to
ensure reproducibility.}
}
\value{
A list containing the estimated objects and the input parameters:
\itemize{
\item \code{input_params}: parameters used during fitting.
\item \code{emission.model}: fitted emission model object.
\item \code{transition.model}: fitted transition model object.
\item \code{duration.model}: fitted duration model object.
}
}
\description{
Fits an HSMM (or HMM) from a matrix or data frame of observations while
optionally incorporating covariates for the transition and dwell-time
components and choosing among different emission families. The function
builds the duration, transition, and emission models via their respective
factories and runs the EM algorithm to estimate the parameters.
}
\details{
The function initializes the maximum dwell times (if provided),
constructs the modular models through their factories, and launches the EM
algorithm to adapt the parameters to the supplied observations.
}
\examples{
\dontrun{
set.seed(123)
obs <- data.frame(x = rnorm(100))
fit <- fit_hsmm(obs, n_states = 2, model_type = "torus",
                max_iter = 10, verbose = FALSE)
}
}
