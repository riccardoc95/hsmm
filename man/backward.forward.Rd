% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/algorithms_bf.R
\name{backward.forward}
\alias{backward.forward}
\title{Backward-Forward Algorithm for Hidden Markov Models (HMM)}
\usage{
backward.forward(fit, Gamma, Pi, K, M)
}
\arguments{
\item{fit}{A numeric matrix (n x S) containing the emission probabilities for each time step and state.}

\item{Gamma}{A 3-dimensional array (n-1 x S x S) of state transition probabilities between time steps.}

\item{Pi}{A numeric vector of length S with the initial state probabilities.}

\item{K}{An integer, the number of latent groups (or sub-models).}

\item{M}{An integer, the number of states per group.}
}
\value{
A list containing:
\describe{
\item{l}{The log-likelihood of the observed data under the model.}
\item{post.pi.aug}{A matrix (n x S) of posterior probabilities for each state at each time step.}
\item{post.bi.pi.aug}{A 3D array (n-1 x S x S) of pairwise posterior probabilities for transitions.}
}
}
\description{
This function performs the forward-backward (also known as backward-forward) algorithm
for a Hidden Markov Model (HMM). It computes the forward (\code{alpha}) and backward (\code{Beta})
probabilities, as well as the smoothed posterior probabilities of the hidden states.
}
\details{
This implementation includes scaling at each time step (using the vector \code{C}) to avoid numerical underflow.
The function iteratively computes forward and backward probabilities and combines them to obtain smoothed
posterior probabilities for both states and transitions.
}
\examples{
# Example usage:
# backward.forward(fit = matrix(runif(100), 10, 10),
#                  Gamma = array(runif(900), c(9, 10, 10)),
#                  Pi = rep(1/10, 10),
#                  K = 2, M = 5)

}
